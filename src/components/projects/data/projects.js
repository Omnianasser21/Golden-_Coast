import healthCareProjects from "./healthCareProjects";
import decorationProjects from "./decorationProjects";
import electricalProjects from "./electricalProjects";
import poolProjects from "./poolProjects";

const projects = [
  ...healthCareProjects,
  ...decorationProjects,
  ...electricalProjects,
  ...poolProjects,
];

export default projects;
